=render 'common/project_tabs'
.row
  .span11
    #issues
      %table.table.table-striped
        %thead
          %tr
            %th #
            %th Status
            %th Priority
            %th Subject
            %th Assignee
            %th Updated
        %tbody
        - @issues.each do |issue|
          %tr
            %td= issue.id
            %td= issue.status.name
            %td= issue.priority.name
            %td= issue.subject
            %td= issue.author.name
            %td= issue.updated_at

