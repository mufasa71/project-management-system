= simple_form_for([@project, @role], :html => {:class => "form-vertical"}) do |f|
  = render :partial => "roles/form", :locals => { :f => f }
  = link_to "Back", settings_roles_project_path(@project)
  = f.button :submit, "Save", :class => "btn btn-success"


