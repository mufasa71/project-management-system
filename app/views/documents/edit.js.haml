- @modal = capture do
  .modal#edit_document
    .modal-header
      = button_tag '×', :class => "close", "data-dismiss" => "modal"
      %h3 Edit document
    .modal-body
      %p
        = simple_form_for([@project, @document], :html => {:class => "form-vertical"}, :remote => true) do |f|
          = render :partial => "form", :locals => { :f => f }
          .modal-footer
            %a.btn{:href => "#", "data-dismiss" => "modal"} Close
            = f.button :submit, "Update", :class => "btn btn-success"

:plain
  $('#edit_document').modal('hide');
  $('#edit_document').remove();
  $('body').append("#{escape_javascript @modal}");
  $('#edit_document').modal('show');
