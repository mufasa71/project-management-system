=render 'common/project_tabs'
.row
  .span11
    = simple_form_for([@project, @document], :html => { :class => 'form-horizontal'}) do |f|
      = render :partial => 'form', :locals => { :f => f } 
      .form-actions
        = f.button :submit
