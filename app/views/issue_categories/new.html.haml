=simple_form_for([@project, @issue_category], :html => {:class => "form-horizontal"}) do |f|
  =render :partial => "issue_categories/form", :locals => {:f => f}
  =link_to "Cancel", settings_issue_categories_project_path(@project), :class => "btn"
  =f.button :submit, " Add", :class => "btn btn-success icon-plus-sign"
