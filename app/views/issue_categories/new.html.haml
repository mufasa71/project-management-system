=render 'common/project_tabs'
.row
  .span12
    #category
      %h3 Add new category
      =simple_form_for([:project, @project.issue_categories.build], :html => {:class => "form-horizontal"}) do |f|
        =render :partial => "issue_categories/form", :locals => {:f => f}
        =f.button :submit, "Add", :class => "btn btn-success"
