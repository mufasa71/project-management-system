%h2 Add groups
= render layout: 'form' do |f|
  .form-controls
    .row
      = f.simple_fields_for :groups, Group.new do |a|
        .row
          .span4
            = a.input :title
          .span2
            = a.link_to_remove " Remove",  :class => "icon-trash"
    .row
      = f.link_to_add "", :groups, :class => "btn btn-mini btn-primary icon-plus-sign", :style => "margin-left: 120px"
