%h2 New project

= simple_form_for @project, :html => { :class => 'form-horizontal' } do |f|
  = render :partial => "projects/form", :locals => { :f => f }
  .form-actions
    = f.button :submit, :class => 'btn-primary btn-large', :value => "Continue >>"
