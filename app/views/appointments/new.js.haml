- @modal = capture do
  .modal#new_appointment
    .modal-header
      = button_tag '×', :class => "close", "data-dismiss" => "modal"
      %h3 New appointment
    .modal-body
      %p
        = simple_form_for([@project, @appointment], :html => {:class => "form-vertical"}, :remote => true) do |f|
          = render :partial => "form", :locals => { :f => f }
          .modal-footer
            %a.btn{:href => "#", "data-dismiss" => "modal"} Close
            = f.button :submit, "Save", :class => "btn btn-success"

:plain
  $('#new_appointment').modal('hide');
  $('#new_appointment').remove();
  $('body').append("#{escape_javascript @modal}");
  $('#new_appointment').modal('show');
