%table#mail-panel
  %tr
    %td
      = link_to " Compose", new_message_path, :class => 'btn btn-success', :remote => true
    %td
      = form_tag search_path, :id => "messages_search", :method => :get do
        %input{ type: 'search', placeholder: 'Search', name: 'search' }
