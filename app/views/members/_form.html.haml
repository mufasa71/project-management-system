= simple_form_for([:project, @member]) do |f|
  .form-inputs
    = f.association :roles, :as => :check_boxes
  = f.button :submit, :value => "Change"
  = link_to "Cancel", settings_members_project_path(:id => @member.project_id)
