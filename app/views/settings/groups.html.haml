=render 'common/project_tabs'
#row
  .span3
    =render 'common/settings_nav'
  .span8
    #settings
      =render :template => 'groups/index', :object => @groups
      =link_to " Add new group", new_project_group_path(:project_id => @project), :class => "btn btn-success icon-plus-sign", :remote => true
